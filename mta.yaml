_schema-version: "3.2"
ID: EHSMPortal
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: EHSMPortal-destination-content
  type: com.sap.application.content
  requires:
  - name: EHSMPortal-destination-service
    parameters:
      content-target: true
  - name: EHSMPortal-repo-host
    parameters:
      service-key:
        name: EHSMPortal-repo-host-key
  - name: EHSMPortal-uaa
    parameters:
      service-key:
        name: EHSMPortal-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: EHSMPortal_html_repo_host
          ServiceInstanceName: EHSMPortal-html5-service
          ServiceKeyName: EHSMPortal-repo-host-key
          sap.cloud.service: EHSMPortal
        - Authentication: OAuth2UserTokenExchange
          Name: EHSMPortal_uaa
          ServiceInstanceName: EHSMPortal-xsuaa-service
          ServiceKeyName: EHSMPortal-uaa-key
          sap.cloud.service: EHSMPortal
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: EHSMPortal-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: EHSMPortal-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - EHSMPortal.zip
      name: EHSMPortal
      target-path: resources/
- name: EHSMPortal
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: EHSMPortal-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: EHSMPortal-destination-service
    service-plan: lite
- name: EHSMPortal-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: EHSMPortal-xsuaa-service
    service-plan: application
- name: EHSMPortal-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: EHSMPortal-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
